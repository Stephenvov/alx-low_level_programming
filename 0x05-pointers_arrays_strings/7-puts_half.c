#include "main.h"
/**
 * puts_half - printing reverse string
 * @s: variable for string
 */
void puts_half(char *s)
{
	int x;
	int half;

	while (*s != '\0')
		x++;
	half = x / 2;

	_putchar(half + '0');
}
